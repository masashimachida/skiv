# SKIV

## Installation

```shell
npm i -g skiv
```

## Usage

### Initialize

cd to your project directory

```shell
npx skiv init
```

generate `.skiv` directory

edit `.skiv/config.yaml` and `.skiv/roles/*`

### Start

```shell
npx skiv start
```
